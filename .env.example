# AI gateway (OpenAI-compatible). Get values from your CognitioLabs candidate page.
# Server-only: never expose to the client bundle.
AI_BASE_URL=
AI_API_KEY=
AI_MODEL=
# Optional: some gateways reject response_format; the code retries without it automatically.
# AI_JSON_MODE=1
